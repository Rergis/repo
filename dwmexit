#!/bin/sh
color='-i -nf #600000 -nb black -sf black -sb #600000'
prompt='-p ?'
opt=$(printf "Reboot\nShutdown\nLogout\nLock" | dmenu $prompt $colo
case $opt in
	'Reboot') 
		sudo reboot ;;
	'Shutdown') 
		sudo poweroff ;;
	'Logout') 
		sudo pkill -KILL -u $(whoami);;
	'Lock') 
		slock ;;
esac
